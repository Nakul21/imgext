name: Deploy Static Site

on:
  push:
    branches: [ web-workers-impl ]
  workflow_dispatch:

# Important: Updated permissions
permissions:
  contents: write
  pages: write
  id-token: write
  deployments: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          ref: web-workers-impl

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Create deployment directory
        run: |
          mkdir -p dist
          cp -r models dist/ || true
          cp -r lib dist/ || true
          cp -r image dist/ || true
          cp *.html dist/ || true
          cp *.js dist/ || true
          cp *.css dist/ || true
          cp *.json dist/ || true
          mkdir -p dist/opencv
          cp opencv/opencv.js dist/opencv/opencv.js 
          cp opencv/opencv_js.wasm dist/opencv/opencv_js.wasm

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
